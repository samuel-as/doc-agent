# doc-agent v0.4 — Recorte inteligente — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cada passo com print ganha, quando o alvo está dentro de um contêiner semântico, um recorte desse contêiner (`shots/step-NNN-crop.png`) e um campo `preferred` que diz ao gerador qual arquivo abrir; o print inteiro continua existindo.

**Architecture:** O script injetado mede `containerRect` no evento (ancestral semântico + margem + tamanho mínimo, `null` se grande demais); a consolidação carrega o rect junto do print do passo; `session.finalize` recorta a imagem final (já marcada e tarjada) com `cropPng` e define `preferred`. A skill abre só o arquivo preferido.

**Tech Stack:** Node.js 22+ (ESM), `node --test`, playwright-core (CDP), pngjs, esbuild (bundle commitado).

**Spec:** `docs/superpowers/specs/2026-09-11-doc-agent-v0.4-recorte-inteligente-design.md`

## Global Constraints

- Código, comentários, testes e docs de usuário em inglês; este plano e a spec em pt-BR.
- Repo público: só `example.com` em exemplos, nunca nomes ou domínios reais.
- Todo comando npm roda dentro de `tools/recorder`.
- Sempre que `tools/recorder/src/` mudar, rodar `npm run build` e commitar `.claude/skills/document/scripts/doc-agent.mjs` e `.claude/skills/document/THIRD-PARTY-NOTICES.md` **no mesmo commit**.
- Sem dependências novas. Sem trailer `Co-Authored-By`. Portas CDP fixas (9333/9334).
- O print inteiro nunca é descartado por causa do recorte. Falha no recorte → `screenshotCrop: null`, `preferred: 'full'`.
- Constantes do recorte: contêineres `form, fieldset, dialog, [role="dialog"], table, [role="tabpanel"], section, article, aside, nav, header`; área máxima 60 % do viewport; lado mínimo visível 40 px; margem 24 px; mínimo 480 × 240 px CSS.
- Branch `dev`.

---

## Mapa de arquivos

| Arquivo | Ação |
|---|---|
| `tools/recorder/src/recorder/marker.js` | adicionar `cropPng` |
| `tools/recorder/src/recorder/instrument.js` | adicionar `containerRect` ao `base()` |
| `tools/recorder/src/recorder/consolidate.js` | carregar `containerRect` no passo |
| `tools/recorder/src/recorder/session.js` | `_renderShot` grava o recorte; `preferred` |
| `tools/recorder/tests/{marker,recorder,consolidate,session}.test.js` | novos casos |
| `tools/recorder/smoke/fixtures/form.html`, `tools/recorder/smoke/driver.mjs` | `fieldset` + asserts |
| `.claude/skills/document/references/write-doc.md`, `README.md`, `CHANGELOG.md` | docs |

---

### Task 1: `cropPng` em `marker.js`

**Files:**
- Modify: `tools/recorder/src/recorder/marker.js` (acrescentar ao fim)
- Test: `tools/recorder/tests/marker.test.js`

**Interfaces:**
- Produces: `cropPng(inputPng: Buffer, rect: {x,y,w,h}): Promise<Buffer>` — recorta à imagem; lança `Error('empty crop')` se o retângulo recortado ficar sem área.

- [ ] **Step 1: testes que falham** (acrescentar ao fim de `tests/marker.test.js`; o arquivo já importa `PNG`, `blankPng` e `pixelAt`)

```js
import { cropPng } from '../src/recorder/marker.js';

function paintedPng(width, height, box) {
  const png = new PNG({ width, height });
  png.data.fill(255);
  for (let y = box.y; y < box.y + box.h; y++) {
    for (let x = box.x; x < box.x + box.w; x++) {
      const i = (width * y + x) << 2;
      png.data[i] = 10; png.data[i + 1] = 20; png.data[i + 2] = 30; png.data[i + 3] = 255;
    }
  }
  return PNG.sync.write(png);
}

test('cropPng returns an image of the rect size with the same pixels', async () => {
  const src = paintedPng(200, 100, { x: 50, y: 20, w: 40, h: 10 });
  const out = await cropPng(src, { x: 40, y: 10, w: 60, h: 30 });
  const meta = PNG.sync.read(out);
  assert.equal(meta.width, 60);
  assert.equal(meta.height, 30);
  assert.deepEqual(pixelAt(out, 10, 10), { r: 10, g: 20, b: 30 });   // (50,20) in the source
  assert.deepEqual(pixelAt(out, 5, 5), { r: 255, g: 255, b: 255 });  // (45,15): white
});

test('cropPng clips the rect to the image bounds', async () => {
  const out = await cropPng(blankPng(100, 100), { x: 80, y: 90, w: 50, h: 50 });
  const meta = PNG.sync.read(out);
  assert.equal(meta.width, 20);
  assert.equal(meta.height, 10);
});

test('cropPng throws on an empty crop', async () => {
  await assert.rejects(() => cropPng(blankPng(100, 100), { x: 150, y: 0, w: 10, h: 10 }), /empty crop/);
  await assert.rejects(() => cropPng(blankPng(100, 100), { x: 0, y: 0, w: 0, h: 10 }), /empty crop/);
});
```

- [ ] **Step 2: rodar e ver falhar**

Run: `cd tools/recorder && node --test tests/marker.test.js`
Expected: FAIL — `cropPng` não exportado.

- [ ] **Step 3: implementar** (acrescentar ao fim de `src/recorder/marker.js`)

```js
// Crops a rectangle out of a PNG. The rect is clipped to the image; an empty result is an
// error so the caller can fall back to the full screenshot. Used AFTER redaction and marker,
// so the crop inherits both.
export async function cropPng(inputPng, { x, y, w, h }) {
  const png = PNG.sync.read(inputPng);
  const x0 = Math.max(0, Math.floor(x));
  const y0 = Math.max(0, Math.floor(y));
  const x1 = Math.min(png.width, Math.ceil(x + w));
  const y1 = Math.min(png.height, Math.ceil(y + h));
  const cw = x1 - x0, ch = y1 - y0;
  if (cw <= 0 || ch <= 0) throw new Error('empty crop');
  const out = new PNG({ width: cw, height: ch });
  for (let row = 0; row < ch; row++) {
    const srcStart = ((png.width * (y0 + row)) + x0) << 2;
    png.data.copy(out.data, (cw * row) << 2, srcStart, srcStart + (cw << 2));
  }
  return PNG.sync.write(out);
}
```

- [ ] **Step 4: rodar e ver passar**

Run: `cd tools/recorder && node --test tests/marker.test.js`
Expected: PASS.

- [ ] **Step 5: build e commit**

```bash
cd tools/recorder && npm run build && cd ../..
git add tools/recorder/src/recorder/marker.js tools/recorder/tests/marker.test.js .claude/skills/document/scripts/doc-agent.mjs .claude/skills/document/THIRD-PARTY-NOTICES.md
git commit -m "feat(recorder): cropPng for container crops"
```

---

### Task 2: `containerRect` no script injetado

**Files:**
- Modify: `tools/recorder/src/recorder/instrument.js` (função `base` e vizinhança de `rectOf`)
- Test: `tools/recorder/tests/recorder.test.js` (testes estáticos)

**Interfaces:**
- Produces: todo payload construído por `base(kind, el)` com `el` não nulo carrega `containerRect: {x,y,w,h} | null`. Payloads com `el` nulo (`drag` no drop, `shortcut`/`enter` sem foco) carregam `containerRect: null`.

- [ ] **Step 1: teste estático que falha** (acrescentar a `tests/recorder.test.js`, junto dos outros estáticos)

```js
test('the injected script measures the semantic container of the target for the crop', () => {
  const src = buildInitScript();
  assert.ok(src.includes('containerRect'), 'containerRect missing from payloads');
  for (const sel of ['form', 'fieldset', 'dialog', '[role="dialog"]', 'table', '[role="tabpanel"]', 'section', 'article', 'aside', 'nav', 'header']) {
    assert.ok(src.includes(sel), `container selector ${sel} missing`);
  }
  assert.ok(src.includes('CROP_MAX_AREA = 0.6'), '60% viewport cap missing');
  assert.ok(src.includes('CROP_MIN_W = 480') && src.includes('CROP_MIN_H = 240'), 'minimum crop size missing');
  assert.ok(src.includes('CROP_MARGIN = 24'), 'crop margin missing');
});
```

- [ ] **Step 2: rodar e ver falhar**

Run: `cd tools/recorder && node --test tests/recorder.test.js`
Expected: FAIL no teste novo.

- [ ] **Step 3: implementar** — em `src/recorder/instrument.js`, logo após a definição de `rectOf`, acrescente:

```js
    // Logical container of the target, for the cropped variant of the screenshot. Only
    // semantic ancestors count; null means "no crop, use the full screenshot". Measured at
    // event time (the capture runs a bit later; a scroll in between shifts the crop — a
    // quality issue, not a privacy one: redaction rects are measured at capture time).
    const CONTAINERS = 'form, fieldset, dialog, [role="dialog"], table, [role="tabpanel"], section, article, aside, nav, header';
    const CROP_MAX_AREA = 0.6; // of the viewport: bigger than this, the crop would not help
    const CROP_MIN_SIDE = 40;  // visible part smaller than this is not a usable container
    const CROP_MARGIN = 24;
    const CROP_MIN_W = 480, CROP_MIN_H = 240;
    const containerRect = (el) => {
      if (!el || !el.closest) return null;
      const c = el.closest(CONTAINERS);
      if (!c) return null;
      const vw = window.innerWidth, vh = window.innerHeight;
      const r = c.getBoundingClientRect();
      let x0 = Math.max(0, r.left), y0 = Math.max(0, r.top);
      let x1 = Math.min(vw, r.right), y1 = Math.min(vh, r.bottom);
      if (x1 - x0 < CROP_MIN_SIDE || y1 - y0 < CROP_MIN_SIDE) return null;
      if ((x1 - x0) * (y1 - y0) > CROP_MAX_AREA * vw * vh) return null;
      x0 -= CROP_MARGIN; y0 -= CROP_MARGIN; x1 += CROP_MARGIN; y1 += CROP_MARGIN;
      const cx = (x0 + x1) / 2, cy = (y0 + y1) / 2;
      const w = Math.max(x1 - x0, CROP_MIN_W), h = Math.max(y1 - y0, CROP_MIN_H);
      x0 = Math.max(0, cx - w / 2); x1 = Math.min(vw, cx + w / 2);
      y0 = Math.max(0, cy - h / 2); y1 = Math.min(vh, cy + h / 2);
      return { x: Math.round(x0), y: Math.round(y0), w: Math.round(x1 - x0), h: Math.round(y1 - y0) };
    };
```

E em `base`, acrescente o campo:

```js
    const base = (kind, el) => ({
      kind: kind, ts: Date.now(),
      label: el ? labelFor(el) : null, selector: el ? cssPath(el) : null,
      scrollY: window.scrollY, viewportH: window.innerHeight,
      sensitiveRects: sensitiveRects(el),
      containerRect: el ? containerRect(el) : null,
    });
```

(Se a `base` atual tiver outros campos, como `hasPasswordField`, mantenha-os; só acrescente `containerRect`.)

- [ ] **Step 4: rodar e ver passar**

Run: `cd tools/recorder && node --test tests/recorder.test.js`
Expected: PASS.

- [ ] **Step 5: build e commit**

```bash
cd tools/recorder && npm run build && cd ../..
git add tools/recorder/src/recorder/instrument.js tools/recorder/tests/recorder.test.js .claude/skills/document/scripts/doc-agent.mjs .claude/skills/document/THIRD-PARTY-NOTICES.md
git commit -m "feat(recorder): measure the semantic container of each action"
```

---

### Task 3: `containerRect` nos passos consolidados

**Files:**
- Modify: `tools/recorder/src/recorder/consolidate.js`
- Test: `tools/recorder/tests/consolidate.test.js`

**Interfaces:**
- Consumes: eventos com `containerRect` (Task 2).
- Produces: todo passo tem `containerRect: {x,y,w,h} | null` (interno, removido em `finalize`), vindo do mesmo evento que forneceu `screenshot`.

- [ ] **Step 1: testes que falham** (acrescentar ao fim de `tests/consolidate.test.js`; o helper `ev` já existe)

```js
const RECT = { x: 10, y: 20, w: 500, h: 300 };
const OTHER = { x: 0, y: 0, w: 480, h: 240 };

test('click/check/select carry their own containerRect; navigation/enter/shortcut have null', () => {
  const steps = consolidate([
    ev('click', { selector: '#a', containerRect: RECT, screenshot: 'shots/raw-001.png' }),
    ev('check', { selector: '#c', value: 'on', containerRect: RECT, ts: 2000 }),
    ev('select', { selector: '#s', value: 'X', containerRect: RECT, ts: 3000 }),
    ev('navigation', { url: 'https://x/2', ts: 4000 }),
    ev('shortcut', { value: 'Ctrl+S', ts: 5000 }),
    ev('enter', { ts: 6000 }),
  ]);
  assert.deepEqual(steps[0].containerRect, RECT);
  assert.deepEqual(steps[1].containerRect, RECT);
  assert.deepEqual(steps[2].containerRect, RECT);
  assert.equal(steps[3].containerRect, null);
  assert.equal(steps[4].containerRect, null);
  assert.equal(steps[5].containerRect, null);
});

test('fill takes the containerRect of the focus event that provided its screenshot', () => {
  const steps = consolidate([
    ev('click', { selector: '#f', isEditable: true, screenshot: 'shots/raw-001.png', containerRect: RECT, ts: 1000 }),
    ev('field-focus', { selector: '#f', ts: 1050, containerRect: OTHER }), // same interaction, no screenshot
    ev('field-commit', { selector: '#f', value: 'v', ts: 3000 }),
  ]);
  assert.equal(steps[0].type, 'fill');
  assert.deepEqual(steps[0].containerRect, RECT);
});

test('fill without any focus screenshot has containerRect null', () => {
  const steps = consolidate([ev('field-commit', { selector: '#f', value: 'v' })]);
  assert.equal(steps[0].containerRect, null);
});

test('drag takes the containerRect of the drag-start', () => {
  const steps = consolidate([
    ev('drag-start', { selector: '#i', screenshot: 'shots/raw-001.png', containerRect: RECT, ts: 1000 }),
    ev('drag', { selector: '#i', target: 'Done', ts: 1500 }),
  ]);
  assert.deepEqual(steps[0].containerRect, RECT);
});
```

- [ ] **Step 2: rodar e ver falhar**

Run: `cd tools/recorder && node --test tests/consolidate.test.js`
Expected: FAIL (`containerRect` undefined nos passos).

- [ ] **Step 3: implementar** — em `src/recorder/consolidate.js`:

Na merge de `field-focus` (dentro de `focusBySelector.set(...)`), acrescente a linha:

```js
          containerRect: shotFrom.containerRect ?? null,
```

No passo `fill`, dentro de `makeStep('fill', ev, {...})`, acrescente:

```js
          containerRect: focus?.screenshot ? (focus.containerRect ?? null) : null,
```

No passo `drag`, dentro de `makeStep('drag', ev, {...})`, acrescente:

```js
          containerRect: start?.containerRect ?? null,
```

Nos passos `navigation`, `enter` e `shortcut`, acrescente `containerRect: null` ao objeto `extra` passado a `makeStep` (o `shortcut` pode ter print de tela inteira, mas não recorta).

Em `makeStep`, acrescente ao objeto base:

```js
    containerRect: ev.containerRect ?? null, // internal: consumed by session.finalize, then dropped
```

- [ ] **Step 4: rodar e ver passar**

Run: `cd tools/recorder && node --test tests/consolidate.test.js`
Expected: PASS (novos e antigos).

- [ ] **Step 5: build e commit**

```bash
cd tools/recorder && npm run build && cd ../..
git add tools/recorder/src/recorder/consolidate.js tools/recorder/tests/consolidate.test.js .claude/skills/document/scripts/doc-agent.mjs .claude/skills/document/THIRD-PARTY-NOTICES.md
git commit -m "feat(recorder): steps carry the container rect of their screenshot"
```

---

### Task 4: recorte gravado em `finalize`, `screenshotCrop` e `preferred`

**Files:**
- Modify: `tools/recorder/src/recorder/session.js` (`finalize`, `_renderShot`, import)
- Test: `tools/recorder/tests/session.test.js`

**Interfaces:**
- Consumes: passos com `containerRect`; `cropPng` (Task 1).
- Produces: passos finais com `screenshot`, `screenshotCrop`, `preferred`; sem `containerRect`, `coords`, `sensitiveRects`.

- [ ] **Step 1: testes que falham** (acrescentar a `tests/session.test.js`; ajuste o helper `ev` para incluir `containerRect: null` no default)

```js
test('a step with a containerRect gets a crop file, screenshotCrop and preferred crop', async () => {
  const root = await fs.mkdtemp(path.join(os.tmpdir(), 'doc-agent-'));
  const session = new SessionWriter(path.join(root, 'docs', 'crop'), 'crop', NOW);
  await session.init();
  await session.addEvent(ev('click', { selector: '#a', coords: { x: 25, y: 25 }, containerRect: { x: 10, y: 10, w: 30, h: 20 } }), await tinyPng());
  const dir = await session.finalize();
  const json = JSON.parse(await fs.readFile(path.join(dir, 'session.json'), 'utf8'));
  assert.equal(json.steps[0].screenshot, 'shots/step-001.png');
  assert.equal(json.steps[0].screenshotCrop, 'shots/step-001-crop.png');
  assert.equal(json.steps[0].preferred, 'crop');
  assert.equal(json.steps[0].containerRect, undefined);
  const crop = PNG.sync.read(await fs.readFile(path.join(dir, 'shots', 'step-001-crop.png')));
  assert.equal(crop.width, 30);
  assert.equal(crop.height, 20);
  // the marker (drawn at 25,25 on the full image) is inside the crop at (15,15)
  const i = (crop.width * 15 + 15) << 2;
  assert.ok(crop.data[i] > 150 && crop.data[i + 1] < 200, 'marker fill missing from the crop');
});

test('a step without containerRect is preferred full and has no crop file', async () => {
  const root = await fs.mkdtemp(path.join(os.tmpdir(), 'doc-agent-'));
  const session = new SessionWriter(path.join(root, 'docs', 'nocrop'), 'nocrop', NOW);
  await session.init();
  await session.addEvent(ev('click', { selector: '#a' }), await tinyPng());
  const dir = await session.finalize();
  const json = JSON.parse(await fs.readFile(path.join(dir, 'session.json'), 'utf8'));
  assert.equal(json.steps[0].screenshotCrop, null);
  assert.equal(json.steps[0].preferred, 'full');
  assert.deepEqual((await fs.readdir(path.join(dir, 'shots'))).sort(), ['step-001.png']);
});

test('a crop that fails falls back to full without touching the full screenshot', async () => {
  const root = await fs.mkdtemp(path.join(os.tmpdir(), 'doc-agent-'));
  const session = new SessionWriter(path.join(root, 'docs', 'badcrop'), 'badcrop', NOW);
  await session.init();
  await session.addEvent(ev('click', { selector: '#a', containerRect: { x: 500, y: 500, w: 10, h: 10 } }), await tinyPng()); // outside the 50x50 image
  const dir = await session.finalize();
  const json = JSON.parse(await fs.readFile(path.join(dir, 'session.json'), 'utf8'));
  assert.equal(json.steps[0].screenshot, 'shots/step-001.png');
  assert.equal(json.steps[0].screenshotCrop, null);
  assert.equal(json.steps[0].preferred, 'full');
});

test('a step with no screenshot at all is preferred full', async () => {
  const root = await fs.mkdtemp(path.join(os.tmpdir(), 'doc-agent-'));
  const session = new SessionWriter(path.join(root, 'docs', 'noshot'), 'noshot', NOW);
  await session.init();
  await session.addEvent(ev('click', { selector: '#a', containerRect: { x: 0, y: 0, w: 10, h: 10 } }), null);
  const dir = await session.finalize();
  const json = JSON.parse(await fs.readFile(path.join(dir, 'session.json'), 'utf8'));
  assert.equal(json.steps[0].screenshot, null);
  assert.equal(json.steps[0].screenshotCrop, null);
  assert.equal(json.steps[0].preferred, 'full');
});
```

- [ ] **Step 2: rodar e ver falhar**

Run: `cd tools/recorder && node --test tests/session.test.js`
Expected: FAIL (`screenshotCrop`/`preferred` undefined).

- [ ] **Step 3: implementar** — em `src/recorder/session.js`:

Import:

```js
import { drawMarker, drawRedaction, cropPng } from './marker.js';
```

No laço de `finalize`, substitua o bloco que monta `finalSteps` por:

```js
    for (const step of steps) {
      const { screenshot, screenshotCrop } = step.screenshot
        ? await this._renderShot(step)
        : { screenshot: null, screenshotCrop: null };
      const { coords, screenshot: raw, sensitiveRects, containerRect, ...rest } = step;
      finalSteps.push({ ...rest, screenshot, screenshotCrop, preferred: screenshotCrop ? 'crop' : 'full' });
    }
```

Substitua `_renderShot` por:

```js
  // Final images of a step: the click marker on top of the already-redacted raw capture,
  // saved as shots/step-NNN.png, plus — when the action happened inside a semantic
  // container — a crop of that container, saved as shots/step-NNN-crop.png. The crop is cut
  // from the FINAL full image, so it inherits marker and redaction. Any failure on the crop
  // leaves screenshotCrop null; any failure on the full image leaves both null — one step
  // without an image must not abort the rest of the recording.
  async _renderShot(step) {
    try {
      let buf = await fs.readFile(path.join(this.dir, step.screenshot));
      if (step.coords) {
        try { buf = await drawMarker(buf, step.coords); } catch { /* a screenshot without the marker beats no screenshot */ }
      }
      const n = String(step.index).padStart(3, '0');
      const screenshot = `shots/step-${n}.png`;
      await fs.writeFile(path.join(this.dir, screenshot), buf);
      let screenshotCrop = null;
      if (step.containerRect) {
        try {
          const cropped = await cropPng(buf, step.containerRect);
          screenshotCrop = `shots/step-${n}-crop.png`;
          await fs.writeFile(path.join(this.dir, screenshotCrop), cropped);
        } catch { screenshotCrop = null; }
      }
      return { screenshot, screenshotCrop };
    } catch {
      return { screenshot: null, screenshotCrop: null };
    }
  }
```

- [ ] **Step 4: rodar a suíte inteira**

Run: `cd tools/recorder && npm test`
Expected: PASS. Se o teste antigo `writes a full session` listar `shots/` com `deepEqual`, ele continua válido: aqueles passos não têm `containerRect`.

- [ ] **Step 5: build e commit**

```bash
cd tools/recorder && npm run build && cd ../..
git add tools/recorder/src/recorder/session.js tools/recorder/tests/session.test.js .claude/skills/document/scripts/doc-agent.mjs .claude/skills/document/THIRD-PARTY-NOTICES.md
git commit -m "feat(recorder): write a container crop per step and mark the preferred image"
```

---

### Task 5: smoke `form` com `fieldset`, skill, README e CHANGELOG

**Files:**
- Modify: `tools/recorder/smoke/fixtures/form.html`, `tools/recorder/smoke/driver.mjs`
- Modify: `.claude/skills/document/references/write-doc.md`, `README.md`, `CHANGELOG.md`

- [ ] **Step 1: fixture** — em `form.html`, envolva os campos, o checkbox, `#saved`, a lista `#queue` e `#done` num `<fieldset id="ticket-fields">` dentro do `<form>`; o `<div class="spacer">` e o `<button id="submit">` ficam fora do `fieldset`, ainda dentro do `<form>`:

```html
  <form action="ok.html" method="get">
    <fieldset id="ticket-fields">
      <legend>Ticket</legend>
      <p><label for="reason">Reason</label> <input id="reason" name="reason" type="text"></p>
      <p><label for="detail">Detail</label> <textarea id="detail" name="detail"></textarea></p>
      <p><label for="type">Type</label>
        <select id="type" name="type"><option>Incident</option><option>Request</option></select></p>
      <p><label><input type="checkbox" id="urgent" name="urgent"> Urgent</label></p>
      <p id="saved"></p>
      <ul id="queue"><li id="item-a" draggable="true">Task A</li><li id="item-b" draggable="true">Task B</li></ul>
      <div id="done">Done</div>
    </fieldset>
    <div class="spacer"></div>
    <p><button id="submit" type="submit">Submit ticket</button></p>
  </form>
```

O restante do arquivo (estilos e script) não muda.

- [ ] **Step 2: asserts no driver** — no ramo `form` da validação (`} else {` após `mode === 'dynamic'`), acrescente antes de `if (failures.length)`:

```js
  // Crop: actions inside the <fieldset> get a container crop; the submit button's container
  // is the whole <form> (taller than 60% of the viewport) and the navigation never crops.
  const reasonFill = fills.find((s) => s.value === 'test ticket');
  check(reasonFill?.preferred === 'crop' && !!reasonFill?.screenshotCrop, `fill of Reason should have a crop: ${JSON.stringify(reasonFill)}`);
  check(chk?.preferred === 'crop' && !!chk?.screenshotCrop, 'check step should have a crop');
  if (reasonFill?.screenshotCrop) {
    const full = await readShot(reasonFill);
    const crop = PNG.sync.read(await fs.readFile(path.join(sessionDir, reasonFill.screenshotCrop.replaceAll('/', path.sep))));
    check(crop.width < full.width && crop.height < full.height, `crop (${crop.width}x${crop.height}) not smaller than full (${full.width}x${full.height})`);
    check(countColor(crop, [224, 36, 94]) > 50, 'marker ring missing from the crop');
  }
  check(submit?.preferred === 'full' && submit?.screenshotCrop === null, `submit should be preferred full: ${JSON.stringify(submit)}`);
  const navStep = session.steps.find((s) => s.type === 'navigation');
  check(navStep?.preferred === 'full' && navStep?.screenshotCrop === null, 'navigation should be preferred full');
```

(`fills`, `chk`, `submit`, `readShot`, `countColor` e `sessionDir` já existem nesse ramo do driver.)

- [ ] **Step 3: rodar o smoke**

Run: `cd tools/recorder && npm run smoke`
Expected: `FORM SMOKE OK`. Se o `fieldset` ultrapassar 60 % do viewport na sua janela e os fills saírem `full`, reduza a altura do `fieldset` (menos parágrafos ou `#queue` menor), não a regra.

- [ ] **Step 4: `write-doc.md`** — substitua a etapa 2 por:

```markdown
2. **Look at every step's image — one file per step.** Each step declares which file to
   open: `preferred: "crop"` → read `<session>/<screenshotCrop>` (the block of the screen
   where the action happened, with the click marker); `preferred: "full"` or absent → read
   `<session>/<screenshot>`. Open ONLY that file. Open the other one only when the user asked
   for it ("use the full screenshot on step 4") or when the crop you opened visibly cut off a
   label or button the instruction needs. The images are the source of truth about what the
   screen shows — use them to:
   - fix generic or truncated labels from the log (the real button/field text is in the image);
   - identify the real name of the screen and of the system;
   - capture messages, warnings and states that the event log does not record.
```

Na etapa 5, troque a primeira frase por: "**Copy the image used in each step** (the crop or the full screenshot, whichever you opened) from `<session>/shots/` to `docs/<slug>/screenshots/`, renaming to `step-NN.png` in the order of the final steps."

Nas **Style rules**, acrescente:

```markdown
- When a step uses a crop and the block's position on the screen is not obvious, say where it
  is ("In the **New ticket** form, …") so the reader can find it on the full page.
```

- [ ] **Step 5: README e CHANGELOG** — em "What you get", na árvore, troque a linha de `shots/` por:

```
    └── shots/step-001.png, step-001-crop.png ...  ← full screenshots + container crops
```

e acrescente após o parágrafo do imperativo:

```markdown
When an action happens inside a form, dialog, table or other semantic block, the recording
also keeps a **crop of that block** with the click marker, and the guide uses it instead of
the whole screen — the full screenshot stays in `sessions/` and is what navigations use.
```

CHANGELOG, em `[Unreleased]` → `### Added`:

```markdown
- Container crops: every action inside a semantic container (`form`, `fieldset`, `dialog`,
  `table`, `section`…) also produces `shots/step-NNN-crop.png` with the marker; steps carry
  `screenshotCrop` and `preferred` (`crop` or `full`), and the skill opens one image per
  step. Containers larger than 60% of the viewport, or absent, keep the full screenshot.
```

- [ ] **Step 6: verificação final e commit**

Run:
```bash
cd tools/recorder && npm test && npm run build && cd ../.. && git status --porcelain .claude/skills/document
cd tools/recorder && npm run smoke:security && npm run smoke && npm run smoke:dynamic && npm run smoke:pdf
```
Expected: testes PASS; `git status` vazio; quatro `SMOKE OK`.

```bash
git add tools/recorder/smoke .claude/skills/document/references/write-doc.md README.md CHANGELOG.md
git commit -m "feat: container crops in the smoke, the skill and the docs"
```

---

## Autorrevisão

**Cobertura da spec:** Seção 1 (contêineres, limites, margem, mínimo, `containerRect` no payload, contrato) → Tasks 2 e 4. Seção 2 (consolidação herdando do print, `_renderShot`, `cropPng`, `preferred`) → Tasks 1, 3, 4. Seção 3 (`write-doc.md`, README, CHANGELOG, testes, smoke com `fieldset`) → Task 5 e os testes de cada task. Limitação de timing registrada no comentário da Task 2.

**Consistência:** `cropPng(buf, {x,y,w,h})` (T1, T4); `containerRect` (T2, T3, T4); `screenshotCrop`/`preferred` (T4, T5); constantes `CROP_*` (T2 e teste estático). Nome do arquivo `step-NNN-crop.png` (T4, T5, spec).

**Placeholders:** nenhum.
